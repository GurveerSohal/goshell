{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "rm -rf ./devDist && rm -rf ./.parcel-cache && parcel serve public/**/*.html --dist-dir devDist -p 3000",
    "build": "parcel build public/**/*.html --dist-dir dist --public-url ./"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.9.3"
  }
}
